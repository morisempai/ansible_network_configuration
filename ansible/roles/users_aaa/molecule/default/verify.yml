---
- name: Verify
  hosts: all
  gather_facts: true
  tasks:
    # The converge step above fails the scenario if the role errors, so
    # reaching this play already proves the role loads and dispatches.
    # Extend with real state assertions — see docs/adding-a-role.md.
    - name: "users_aaa | role converged without error"
      ansible.builtin.assert:
        that:
          - true
        success_msg: "'users_aaa' role applied cleanly on the test host."
        fail_msg: "'users_aaa' role verification failed."
