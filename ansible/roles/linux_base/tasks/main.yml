---
# Linux-only role: no vendor dispatch needed, but the file name keeps parity
# with the dispatcher convention used by network roles.
- name: "Linux_base | apply Linux baseline"
  ansible.builtin.include_tasks: linux.yml
