---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: "windows_ad | AD-Domain-Services feature is installed"
      ansible.windows.win_feature:
        name: AD-Domain-Services
        state: present
      check_mode: true
      register: _ad_feature
      failed_when: _ad_feature is changed
